# Authentication & Authorization: [Project Name]

Authentication architecture, token strategy, session model, and authorization rules for [Project Name].

---

## 1. Authentication Methods

| Method | Provider | Audience | Status |
| :--- | :--- | :--- | :--- |
| [e.g. OAuth 2.0 + OIDC] | [e.g. Google, GitHub, Auth0] | [e.g. All users] | [Active \| Planned] |
| [e.g. API Keys] | Internal | [e.g. Service accounts] | [Active \| Planned] |
| [e.g. Email + Password] | Internal | [e.g. All users] | [Active \| Planned] |

---

## 2. Token Strategy

### Access Tokens
- **Format**: [JWT \| opaque \| PASETO]
- **Lifetime**: [15 minutes]
- **Claims**: `sub`, `iat`, `exp`, `scope`, `role`
- **Signing**: [RS256 via JWKS endpoint \| HS256 with shared secret]

### Refresh Tokens
- **Format**: [opaque]
- **Lifetime**: [7 days \| 30 days]
- **Rotation**: [Enabled \| Disabled]
- **Storage**: [HttpOnly cookie \| Secure local storage \| header only]

### API Keys (if applicable)
- **Format**: [prefixed opaque string, e.g. `sk_live_XXXX`]
- **Transmission**: [Bearer header `Authorization: Bearer sk_XXX`]
- **Validation**: [Database lookup + cache \| HMAC verification]

---

## 3. Session Model

- **Type**: [Stateful (server-side session store) \| Stateless (JWT only)]
- **Store**: [Redis \| Database \| Memory (dev only)]
- **Concurrency**: [Single session per user \| Multiple sessions allowed]
- **Inactivity timeout**: [e.g. 30 minutes]
- **Absolute timeout**: [e.g. 24 hours]

---

## 4. Authorization Rules

### Role Hierarchy
| Role | Permissions |
| :--- | :--- |
| `admin` | Full access, user management, system config |
| `user` | CRUD own resources, read shared resources |
| `viewer` | Read-only access to assigned resources |

### Route / Endpoint Protection
| Route | Auth Required | Roles | Notes |
| :--- | :--- | :--- | :--- |
| `GET /api/public/*` | No | — | Public endpoints |
| `GET /api/users/:id` | Yes | `user`, `admin` | Own profile only unless admin |
| `DELETE /api/admin/*` | Yes | `admin` | Admin-only operations |

---

## 5. Security Controls

- **Password policy**: [min length, complexity requirements]
- **Rate limiting**: [e.g. 5 failed login attempts → 15 min lockout per IP]
- **MFA / 2FA**: [TOTP \| WebAuthn \| SMS \| Not yet implemented]
- **Breached password check**: [Enabled via HaveIBeenPwned API \| Not implemented]
- **CSRF protection**: [Double-submit cookie \| SameSite cookies \| Token header]
- **CORS**: [Allowed origins, methods, headers]

---

## 6. Key Management

- **Signing keys**: [Generated by auth provider \| Managed via JWKS rotation]
- **Rotation schedule**: [e.g. Every 90 days, with 7-day overlap]
- **Revocation**: [JWKS key removal \| Token blacklist in Redis \| Not implemented]
- **Secrets storage**: [Environment variables \| Vault \| Cloud KMS]

---

## 7. Audit & Logging

- **Events to log**: login success/failure, token refresh, role changes, account deletion
- **Log format**: [structured JSON \| syslog]
- **Retention**: [90 days \| 1 year]
- **PII masking**: [Mask/hash emails and IPs in logs]

---

## 8. Architectural Decisions

| ADR ID | Decision Title | Status | Date |
| :--- | :--- | :--- | :--- |
| [ADR-XXXX](docs/adr/XXXX-auth-strategy.md) | Auth strategy selection | [Proposed \| Accepted] | YYYY-MM-DD |
| [ADR-XXXX](docs/adr/XXXX-token-format.md) | Token format and signing | [Proposed \| Accepted] | YYYY-MM-DD |

---

## Related Docs

- `ARCHITECTURE.md` — system boundaries, ingress/egress
- `API.md` — endpoint protection, rate limits
- `SECURITY.md` — vulnerability reporting
- `docs/adr/` — architecture decision records
